# 복선추적기 동결 — 복구 범위 산정 v1.0

작성 2026-09-14. 대상: `복선추적기_SNAP-068 ~ SNAP-101` (34개).
선행 문서: `상태조회도구_설계안_v1.0.md` §0.

---

## 1. 결론 먼저

| | |
| --- | --- |
| **근거 보존** | **100%** — 34개 회차 전부 Gate-E 상태반영서 · 회차델타 · 승인 본문 존재 |
| **정보 소실** | **없음** — 델타에 구조화된 `foreshadow:` 블록 202건이 살아 있다 |
| **현재 상태(SNAP-142) 오염** | **제한적** — 이어지는 복선은 정상. **새로 심긴 복선이 누락** |
| **실질 복구 대상** | **구조화 항목 약 93건** (33개 회차) |
| **기계 자동화** | **불가** — 항목당 노출 허용/금지 판정이 필요하다 |

**2026-08-21 「2차 동결 복구」는 절반만 수행됐다.**

---

## 2. 2026-08-21에 실제로 일어난 일

복구 기록은 `ep_log_068_102:` 블록을 **SNAP-102 이후 전 파일에 추가**하는 것으로
끝났다. 확인된 내용:

```yaml
retired: {}

# ★ 2026-08-21 복구 — EP068~102 회차별 복선 요약(Gate-E 상태반영서 원문)
ep_log_068_102:
  EP068: '**FS-놓침 개시** / FS-학교파손 / FS-혜정인지 **진전 0** / FS-간격 4 / FS-못 센 것'
  EP069: '**FS-여론 개시** / **FS-군자료 오독** / FS-몸살 산발 ② / …'
  …
  EP101: ''
  EP102: ''
```

- 수록 회차 33개 · 약어 항목 **173건**(굵게 표시 = 신규·중대 진전 **93건**)
- SNAP-102 ~ SNAP-142 전 파일에 동일하게 존재(35개 키)

**하지 않은 것 두 가지:**

1. **34개 과거 스냅샷을 다시 쓰지 않았다.** SNAP-068~101은 여전히 첫 줄
   주석만 다르고 페이로드가 바이트 동일하며, `as_of_episode: 67` ·
   `last_updated_by: EP067 「표」`로 찍혀 있다.
2. **약어를 구조화 항목으로 승격하지 않았다.** `active:` 아래
   `status` · `next_window` · `allowed_reveal_now` · `prohibited_reveal_now`를
   갖춘 정식 엔트리가 되지 못했다. 즉 **조회도 검증도 안 되는 상태**다.

---

## 3. 현재 상태 오염도 — 생각보다 얕다

SNAP-142 구조화 항목의 `planted_at` 분포:

```
EP001~067 :  16건   (정상 누적)
EP068~099 :   1건   ← 구멍 (EP089 하나뿐)
EP100~142 : 133건   (정상 · 회차당 3~5건)
```

아크별로 보면 경계가 깨끗하다:

| 아크 | 범위 | 구조화 신규 |
| --- | --- | --- |
| S10 | EP62~76 | 7건 — **전부 EP62~67에 심김. EP68~76 구간은 0** |
| **S11** | **EP77~92** | **0건 — 16화 전체가 구조화 0** |
| S12 | EP93~103 | 9건 — **전부 EP101~102에 심김. EP93~100 구간은 0** |
| S13 | ~ | 48건 (정상) |
| S14 | ~ | 48건 (정상) |

**좋은 소식:** 그 구간을 통과해 살아남은 복선은 멀쩡하다.
`FS-S10-INTERVAL`(planted 62 → last_progressed 102) ·
`FS-S10-OPINION`(→96) · `FS-S10-PLAN-SHEET`(→100) 등 **이어지는 스레드의
진전 기록은 EP102 재구축 때 정상 반영됐다.**

**나쁜 소식:** 그 구간에서 **새로 심긴** 복선이 통째로 빠졌다.
`FS-S11-*`가 0건인 것이 가장 선명한 증거다 — 여름방학 아크 16화가
구조화 복선을 하나도 남기지 못했다.

---

## 4. 근거 실태 — 전수 확인 완료

EP068~101 34개 폴더 전부 동일 구성:

```
Black_Titan_EPnnn_nn화_제목_v1.0_GateD승인.md
Black_Titan_EPnnn_Gate-D_검증서_v1.0.md
Black_Titan_EPnnn_Gate-E_상태반영서_v1.0.md      ← 반영 내역 기록
Black_Titan_EPnnn_Level6_내용잠금-문체검색_v1.0.md
Black_Titan_EPnnn_회차델타_v1.0.yaml             ← foreshadow: 블록
Black_Titan_EPnnn_회차브리프_v0.n.md
_superseded/
```

회차델타의 `foreshadow:` 항목 수 (EP068~101 합계 **202건**):

```
068:5  069:6  070:5  071:5  072:6  073:5  074:6  075:5  076:6
077:5  078:5  079:5  080:5  081:6  082:6  083:6  084:7  085:5
086:6  087:5  088:6  089:7  090:8  091:8  092:8  093:6  094:6
095:7  096:8  097:7  098:7  099:7  100:7  101:0
```

**EP101만 예외** — 델타에 `foreshadow:` 키가 없다
(`state_changes`/`enemy`/`terrain`/`canon_new`/`author_device`/`seals_verified`
구성). `ep_log`의 `EP101: ''`와 일치하므로 **누락이 아니라 실제 부재**다.

델타 항목은 약어보다 풍부하다. EP068 예시:

```yaml
foreshadow:
  - "**★★ FS-놓침: 개시(68).** 자기 판정과 실제 구조의 간극. **EP69 §1-E를 지탱한다**"
  - "**FS-학교파손(68).** → **EP69 임시 교실**"
  - "**FS-혜정인지: 진전 0**(EP67 단계 유지). 오늘 혜정은 학교 얘기만 했다"
```

`planted_at` · `surface_reading` · `next_window`는 여기서 바로 뽑을 수 있다.
**`allowed_reveal_now` / `prohibited_reveal_now`는 뽑을 수 없다** — 이게
자동화를 막는 지점이다(§6).

---

## 5. 복구 선택지 세 가지

### A안 — 정직하게 표시만 (0.5시간 · 기계적)

34개 파일에 동결 사실을 명시한다.

```yaml
as_of_episode: 67        # 유지
payload_frozen: true
frozen_note: "EP068~101 구간 미반영. 근거는 각 회차 회차델타 foreshadow 블록."
```

> **주의: 스탬프만 068~101로 고치는 것은 금지.** 내용이 EP067인 파일에
> EP101 도장을 찍으면 **동결이 정상으로 위장된다.** 2026-08-21에 주석만
> 고친 것이 정확히 그 실수였다.

- 얻는 것: `bt_doctor` 검사 2가 오탐 없이 통과. 다음 사람이 안 속는다
- 못 얻는 것: 조회 불가는 그대로

### B안 — 현재 상태만 보강 ★권장 (3~5세션 · 사용자 판단 필요)

**34개 과거 파일은 건드리지 않는다.** 대신 SNAP-142의 `active:`에
EP068~101 신규 복선을 구조화 항목으로 승격한다.

- 입력: 33개 회차델타 `foreshadow:` 블록 (202건)
- 작업: 이미 표현된 것 걸러내고 → 신규 약 93건을 정식 엔트리로 작성
- 우선순위: **S11(EP77~92) 16화가 0건이므로 여기부터**

근거: 과거 스냅샷을 여는 일은 드물다("EP80 시점 상태가 뭐였나"). 반면
현재 상태는 **집필 때마다** 읽는다. 같은 노력으로 얻는 값이 가장 크다.

### C안 — 전량 재구성 (10세션 이상)

SNAP-067에서 출발해 델타 34개를 순차 적용, 068~101을 다시 만든다.
가장 정확하지만 비용이 B안의 3배 이상이고, **B안을 끝내야 C안의 각
중간 상태가 정의된다.** 단독으로 먼저 할 일이 아니다.

---

## 6. 왜 스크립트로 못 돌리는가

델타에서 기계적으로 뽑히는 것:

| 필드 | 자동 | 근거 |
| --- | --- | --- |
| `planted_at` | ○ | 델타의 `(68)` 표기 |
| `surface_reading` | ○ | 항목 본문 |
| `next_window` | △ | `→ EP69` 화살표가 있을 때만 |
| `last_progressed` | ○ | 마지막 언급 회차 |
| `name` · `line` · `secret_layer` | ✗ | 명명·계통 배정은 작가 판단 |
| **`allowed_reveal_now`** | **✗** | **지금 어디까지 써도 되는지** |
| **`prohibited_reveal_now`** | **✗** | **무엇이 봉인인지** |

마지막 두 필드가 이 추적기의 실사용 목적 전부다. 이걸 추정으로 채우면
**틀린 봉인 정보를 정본으로 만드는 것**이라 동결보다 나쁘다.

→ **스크립트는 델타에서 초안을 뽑아 나열하는 데까지. 판정은 사용자.**

---

## 7. 권고 순서

| | 작업 | 비용 | 상태 |
| --- | --- | --- | --- |
| 1 | `bt_doctor.py` 검사 1·2 | 소 | **완료 2026-09-14** |
| 2 | **A안** 34개 파일 동결 표시 | 0.5h | **완료 2026-09-14** |
| 3 | 델타→초안 추출 스크립트 | 소 | 미착수 · 202건을 표로 뽑아만 둔다 |
| 4 | **B안 S11 구간**(EP77~92) | 1~2세션 | 미착수 · 0건 구간. 최우선 |
| 5 | B안 나머지(EP68~76 · 93~100) | 2~3세션 | 미착수 |
| 6 | C안 | — | 보류 권장 |

4번부터가 사용자 판단이 필요한 본 작업이다.

### 1·2번 실행 결과 (2026-09-14)

적발 → 표시 → 재검사까지 마쳤다. 상세는 `상태조회도구_설계안_v1.0.md` §4.

```
표시 전 : 총 실패 68건 (스탬프 34 + 동결 34, 전부 복선추적기 068~101)
표시 후 : 총 실패  0건 / 확인된 동결 34건
--strict: 총 실패 34건  (표식 무시 · 날것의 그림)
```

**교정 1건.** 산정 과정에서 「SNAP-067과 068은 761줄 다르므로 068에는
EP068 내용이 있다」고 본 것은 틀렸다. 그 차이는 **서식뿐이고 데이터는
동일**하다(`yaml.safe_load` 정규화 비교로 확인). 068도 EP067 단계다 —
34개라는 개수와 §3의 결론은 그대로다.

**부수 발견.** `05_문체_상태_인계`는 git 추적 대상이 아니다(추적 파일 0개).
이번 수정은 `99_묶음백업/2026-09-14_복선추적기_동결표시_전/`에 원본을
남겼지만, 이 폴더 전체가 되돌릴 수단 없이 굴러가고 있다. 추적 편입을 권한다.

---

## 8. 덤 — 같이 발견된 것

**인물등장장부 헤더 드리프트.**
`Black_Titan_인물등장장부_v2.37_EP142기준.md` 내부:

- 제목줄: `인물 등장 장부 v1.52`
- 표 문서 ID: `v2.37`
- 표 기준 시점: `EP001~139 승인 본문`
- 파일명: `EP142기준`

세 군데가 서로 다르다. 설계안 §2 검사 4(헤더 드리프트 WARN)가 잡을 대상이다.
복선추적기 건과 원인이 같다 — **마감 스크립트가 일부 필드만 치환하고
나머지는 사람 손에 맡겨져 있다.**
